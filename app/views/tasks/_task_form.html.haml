.TaskForm
  %div{style: "width: 300px;"}
    = form_with(model: @task, local: true) do |f|
      %div
        %p= f.label :title, 'タスク名'
        = f.text_field :title, size: 50
      %div
        %p= f.label :description, '詳細'
        = f.text_area :description
      = f.submit 'Submit'
  %p= link_to '一覧に戻る', root_path
  .TaskMotivate
    偉人の名言とかそれっぽい画像
